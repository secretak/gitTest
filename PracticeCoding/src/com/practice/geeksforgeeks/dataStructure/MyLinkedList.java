package com.practice.geeksforgeeks.dataStructure;

import com.practice.geeksforgeeks.dataStructure.QueueImplementWithLL.Node;


public class MyLinkedList<E> {
	Node head;
	
	/*
	 * public void add(E data) { Node toAddNode = new Node(data);
	 * 
	 * if(isEmpty()) { head = toAddNode; return; } }
	 */

}
